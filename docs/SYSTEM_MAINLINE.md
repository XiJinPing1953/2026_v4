# 系统改造主线与任务总表

更新：2026-09-08。负责人任务：`01a071ca-9c43-7df3-97b4-96f1e0870ab0`（诊断并优化CRM系统与工作流）。用户本日明确要求回到系统诊断主线，由主任务拆解、设计、派发和验收。此页保存当前执行安排；业务含义仍以[领域索引](../state/INDEX.md)为准。

## 主线与已关闭的支线

第一阶段目标仍是减少无法解释的账差与重复返工，并完成一条可恢复的灌装链路。客户源单核对是为通用规则提供依据和反例。

- 两个指定客户的流量、收款、结清状态已完成修正与上线验收，见[第一客户](../state/handoffs/2026-09-07-customer-accounting-reconciliation.md)、[第二客户](../state/handoffs/2026-09-08-haonuo-rebuild.md)。本轮不再次建账、不扩展人工逐户对账。第二客户期初预付款来自用户确认的余额表反推，不能改称已有独立收款凭证。
- 通用期间汇总、实际收款、期初预付款来源识别已上线。金额权威模块、灌装持久处理及其他可信度候选仍需完成独立发布验收。
- STATE压缩、历史归档、领域入口已完成。下一步维护有效决定和任务状态，不再次组织全量历史阅读。

## 基线与变更边界

| 对象 | 派发时已核实版本 | 用途 |
|---|---|---|
| 生产H5与当前客户结算补丁 | `101068bb9580b6c0faa1b9b83886789d8b5c2de3` | 必须保留已上线的账务修复；首客户专用函数另见其交接 |
| 主工作区候选 | `d9eb4fb9dc8edff82789a86a89631f4f86f70b20` | 新任务技术基线；包含未上线的可信度改造 |
| 默认main | `90336801cf67d7c31b9df7104de3c1545452285f` | 已落后，不能当成最新生产或候选 |

新工作区必须核对并继承已确认基线，不能用旧main重做。主任务独占主工作区、STATE、规则入口和最终发布目录；独立任务在各自工作区交付小范围提交。合并按提交和文件审查，不整包覆盖生产。

## 当前三个任务

| 编号与任务 | 模型 | 首批交付物 | 验收负责人 | 当前状态 |
|---|---|---|---|---|
| T1 统一账务规则上线收口 | GPT-6 Astra，极高（xhigh） | 生产/候选差异、规则归属、阻断缺陷补丁、跨入口反例、最小发布组合 | 主任务 | 已派发，独立工作区已到d9eb4fb；尚未验收 |
| T2 灌装保存与异常处理闭环 | GPT-6 Astra，极高（xhigh） | 恢复流程审查、可复现问题补丁、云端演练工具与环境条件、前端/PDA切换清单 | 主任务 | 已派发，独立工作区已到d9eb4fb；尚未验收 |
| T3 发布基线与验收证据整理 | GPT-5.6 Sol，Ultra | 新反例接入统一检查、源码/产物依赖核查、当前可靠性记录、发布验收清单 | 主任务 | 已派发，独立工作区已到d9eb4fb；尚未验收 |

T1拥有账务模块及相关测试；T2拥有灌装/异常/流转及专属测试和schema；T3拥有发布/验证脚本与可靠性记录。公共文件变更先列出交界，由主任务归并。所有任务先核实现有实现，再修复证实的问题，不以拆文件数量衡量完成。

任务窗口创建回执与工作区（创建回执不是可传给任务读取工具的正式任务ID）：

- T1：`client-new-thread:e06c6cfe-1dca-443e-b915-480e74c60c69`；工作区 `/Users/wangbo/.codex/worktrees/6f7e/2026_v4`，分支 `codex/mainline-accounting-20260908`。
- T2：`client-new-thread:2a9a685f-91db-4989-9a4a-90aa61e72cf7`；工作区 `/Users/wangbo/.codex/worktrees/0f60/2026_v4`，分支 `codex/filling-consistency-20260908`。
- T3：`client-new-thread:90ada84d-6d12-4a6d-8b93-d46c959d36cd`；工作区 `/Users/wangbo/.codex/worktrees/d0a5/2026_v4`，分支 `codex/mainline-release-evidence`。

应用返回正式任务ID后替换上述回执；避免因任务列表暂未显示而重复派发。工作区仅用于定位，后续是否仍在运行以任务状态及交付证据为准。

## 目标架构与责任归属

| 层次 | 权威来源与职责 | 首阶段边界 |
|---|---|---|
| 原始事实 | 销售、流量、收款、分配、期初及各自审计 | 修账独立执行；展示变化不自动改写历史 |
| 业务规则 | saleAccounting、periodSummary、receiptSource；bottleFlowRules | 金额、现金、期初和有效流转各有唯一解释；部署副本检查一致 |
| 完整取数 | financialRead与流转完整事件读取 | 限额、缺页、可检测变更必须显式未完成；不声称跨集合原子快照 |
| 可恢复执行 | fillingOperations持久任务、源版本、租约及幂等 | 首先覆盖新灌装；销售多对象写入和完成后历史改单另排 |
| 查询与展示 | 后端返回来源、规则版本、期间和完成状态；前端显示 | 界面不自行猜金额或补零，旧请求不能覆盖新查询 |
| 交付证据 | 源码、依赖清单、产物、环境、运行版本、业务验收 | 已提交、测试通过、部署成功、线上验收分开记录 |

账务的一条事实可以被多处读取，但不能在每个入口再作一次业务归属决定。灌装一次提交可以分步处理，但只能有一个可查询的处理进度。原始客户明细留在受控本地证据目录，任务间只传最小必要数据和可复现的合成反例。

## 推进顺序与完成标准

1. **收齐三个任务的首批交付。** 主任务检查各自基线、修改范围、代码差异和失败反例，退回没有证据或越界的结论。无阻断缺陷时直接进入下一步，不增加结构改造。
2. **先交付通用账务规则。** 以已发布客户修复为保护条件；销售、结算和导出对同一来源一致，期初不会变成新收款，不完整读取不会给出可信总数。核清最小发布范围后，由主任务统一发布并读取真实业务结果。首页/催收只对本次实际覆盖的指标宣称完成。
3. **再交付灌装闭环。** 本地反例通过后，在隔离可恢复范围验证真实云事务、触发器、53瓶中断续跑和幂等。前后端及PDA按兼容条件成套切换，生产真实瓶不充当未经安排的测试对象。测试环境或现场条件缺失时，记录具体条件并继续其他独立工作。
4. **发布后独立核对。** 运行版本对应源码及产物；两家客户的已确认行为不退回；关键源数据前后可比较。测试通过不能替代生产验收。
5. **验收后决定扩展。** 再安排销售多对象写入、历史改单恢复、催收流量债权和汇总提速；本阶段不做全仓重写、技术栈迁移或硬件路线更换。

## 总控与验收契约

- 负责人维护目标、不变量、依赖、文件边界和完成定义，负责跨任务决策、合并与发布。用户决定业务含义及确实缺证据的账务归属，不负责在任务窗口之间搬运上下文。
- 每个子任务返回：提交版本、修改文件、已运行的相关验证、未完成事项、下一条可执行动作；根任务独立复核后才能把状态改为已验收。
- 新发现先判断是否阻断当前验收；不阻断则进入后续列表。确需修正客户事实时单独任务留证，修完回到原里程碑。
- 常规工程选择自主执行。需要用户提供新业务事实时，问题必须关联具体未决点；不能用笼统“是否继续”代替推进。
- 四项成效仍按[可靠性定义](SYSTEM_RELIABILITY.md)采集：同根因返工、未解释账差、未完成同步、版本不匹配。无完整测量写未测量，不从测试数推算改善。

## 后续列表

销售持久写入；完成后的历史改单恢复；跨模块瓶状态竞争；催收覆盖流量与全部债权；收款分配/查询按业务边界拆分；按瓶快照/客户日汇总；PDA和现场设备专项验收。进入实施前，先通过对应前置里程碑，避免三个窗口同时扩展同一核心模块。

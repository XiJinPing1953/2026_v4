# test0314 导入回退说明（env-00jxuffegf2n）

## 批次标识

- `remark`: `import:test0314:env-00jxuffegf2n:202603141050`
- `_id` 列表文件：`docs/test0314.rollback_ids.json`

## 回退方式（推荐）

在 uniCloud 控制台（支付宝云空间 `env-00jxuffegf2n`）中，对 `crm_bottles` 集合按 `remark` 过滤后批量删除：

```js
// 先查数量
await db.collection('crm_bottles').where({
  remark: 'import:test0314:env-00jxuffegf2n:202603141050'
}).count()

// 再删
await db.collection('crm_bottles').where({
  remark: 'import:test0314:env-00jxuffegf2n:202603141050'
}).remove()
```

也可直接使用 [docs/test0314.rollback.batch.js](/Users/wangbo/Downloads/2026_v4/docs/test0314.rollback.batch.js)：
- 默认 `CONFIRM_DELETE=false` 仅统计，不会删除；
- 确认无误后改成 `true` 再执行。

## 回退方式（精确到 ID）

使用 `docs/test0314.rollback_ids.json` 中的 `_id` 列表分批删除（每批建议 <= 200 条）：

```js
db.collection('crm_bottles').where({
  _id: db.command.in(['imp0314_00001', 'imp0314_00002'])
}).remove()
```

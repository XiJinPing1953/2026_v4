# env-00jxuffegf2n 执行清单（先回退，再按 bottle_no 更新）

## 0. 前提

- 云空间：`env-00jxuffegf2n`（支付宝云）
- 集合：`crm_bottles`
- 本次批次标识：`import:test0314:env-00jxuffegf2n:202603141050`

## 1. 回退批次（先 dry-run）

1. 打开云数据库控制台，切到 `crm_bottles`。
2. 粘贴并运行 [test0314.rollback.batch.js](/Users/wangbo/Downloads/2026_v4/docs/test0314.rollback.batch.js)。
3. 观察输出：
- `matched_before_delete`：命中条数；
- 出现 `dry-run only...` 说明尚未删除（安全）。

## 2. 执行回退删除

1. 在同一脚本里把 `CONFIRM_DELETE = false` 改为 `true`。
2. 再运行一次。
3. 结果应满足：
- `matched_after_delete` 为 `0`（或非常接近你的预期）。

## 3. 按 bottle_no 更新（先 dry-run）

1. 打开 [test0314.upsert.by_bottle_no.js](/Users/wangbo/Downloads/2026_v4/docs/test0314.upsert.by_bottle_no.js)。
2. 保持：
- `DRY_RUN = true`
- `ALLOW_CREATE = false`
- `SAVE_BACKUP = true`
3. 运行后检查返回 `summary`：
- `missing` 应为空或在你预期范围内；
- `duplicated_bottle_no` 应为空；
- `failed`、`backup_failed` 应为空。

## 4. 正式执行更新

1. 把 `DRY_RUN = true` 改为 `false`。
2. 再运行一次。
3. 结果应满足：
- `updated` 接近 `matched`；
- `created` 必须为 `0`（因为 `ALLOW_CREATE=false`）。

## 5. 回滚更新（如需要）

- 因 `SAVE_BACKUP=true`，更新前原文档会写入集合 `crm_bottles_import_backups`。
- 可按 `batch_remark` 过滤对应备份，逐条恢复到 `crm_bottles`。

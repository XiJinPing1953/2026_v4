# 已验收账务回合 main

更新时间：2026-09-09。用户批准分批合并：先合入已上线账务，再整理已完成的上下文、发布检查与验收记录；未完成云端/现场验收的灌装、PDA、导入及schema继续留开发分支。本次没有部署或业务数据写入。

## 来源与范围

- 原main：`90336801cf67d7c31b9df7104de3c1545452285f`，合并前与origin/main一致。
- 完整继承正式发布分支`codex/accounting-release-20260909`的`5021753b6e03a938c371357c63ce0f533082a52c`；生产仍以该提交与构建`1788928538096-1d9dfc98`为准，见[线上验收](2026-09-09-accounting-release-accepted.md)。
- 从`codex/system-trust-foundation`的`659f1eb`选取根入口、规则、领域/交接/历史资料、上下文检查、只读取证脚本，以及`bb45947`/`2fa23e5`已完成的网页证据检查；不整支合并开发分支。
- 所有运行代码、数据库结构、原生插件、网关源码及依赖锁与5021753保持一致；保留其8个现网ACL文件差异和规范源码哈希。仅package命令调整，不改变依赖。
- main默认检查为当前上下文、归档完整性、账务反例及既有四函数发布范围；部署清单和范围保护沿用5021753，数据库变更为空。开发分支继续全仓检查，main的显式全仓诊断能力保留，不以自动生成补齐未发布代码或schema。
- STATE按main实际代码重写；开发候选决定明确标注所在分支，缺失实现的资料链接指向固定开发提交。历史STATE完整原文及其校验值不变。

## 验收与推送

整合在独立工作区`/private/tmp/crm-main-integration-20260909`进行，采用正常前进合并，保留发布分支历史。包含本记录的main提交是仓库整合版本，线上版本仍为5021753。

- `npm test`通过78项：账务/发布/只读取证72项，上下文6项；18份当前资料、43行STATE和1份原始归档通过检查。
- web/cloud按正式四函数范围检查通过。原始运行目录对5021753差异为0，包依赖不变，33个函数文件逐个SHA-256匹配正式发布清单。
- 全仓生成或全量部署均未执行，旧schema差异及开发候选没有为通过检查而被带入；本次不会因Git合并重新切换线上。
- 本地核对记录位于 `outputs/trust-audit/2026-09-09/main-integration/`。上下文与网页反例使用本次整合树运行；生产验收继续引用既有回读记录，不伪造新的线上测试。

## 后续

新任务默认从main开始。灌装/PDA/导入任务明确选择开发分支及其候选工作区，完成真实云事务、调度、监管隔离和现场验收后再单独回合。正式发布分支保留在5021753，不能把新的仓库合并提交冒称为当前线上构建。本地原始资料、5份无关本地文件保持原位，不进提交。

# 2026-09-08 三任务主线总验收

主任务直接读取了三个工作区的源码、提交、交接和验证材料，并完成独立复核与工程合并。跨任务主动报告被拦截不影响本次读取，无需用户转述。**代码候选已验收，本批尚未上线；没有改写云端账务或设备数据。**

## 交付与合并

| 任务 | 独立交付 | 主工作区合并 | 结论 |
|---|---|---|---|
| T1账务 | `c28b930`，实现`3fb53ca`；[交接](2026-09-08-mainline-accounting.md) | `8816f1e` | 金额精度、现金来源、读取及业务证据完整性、历史差额凭证说明已验收；通用独立发布待做 |
| T2灌装 | `3acc29a`，实现`6829159`；[交接](2026-09-08-mainline-filling.md) | `bec921d`，含主任务两处追加修复 | 源单确认、恢复、版本保护及事件完整读取已验收；PDA/导入/真实云端条件仍阻断发布 |
| T3发布 | `0076071`；[交接](2026-09-08-mainline-release.md) | `c684c90` | 新反例纳入统一检查、发布缺依赖拒绝、生产证据复核及可靠性范围记录已验收 |

主任务另提交`d7bdae5`：按单一源码同步灌装、异常、第二客户专用修正三个financialRead副本，并把mainlineAccounting、accountingLedgerPrecision、accountingReconciliationIntegration纳入统一测试入口。公共文件交界已归并，没有把专用修账函数列为本批需重新运行的任务。

最终代码提交：`bec921d3efe65e802af0835120cf04eda6c15b15`，分支`codex/system-trust-foundation`。后续STATE/总表/本交接单独提交，不改变该构建的源码归属。原有alarm工具和4份罐日志保持未跟踪，不进入交付。

## 独立复核发现及修复

1. **整车重建遇锁会跳过尚未扫描车辆。** 原rebuildV2推进truckAfter，即使内部返回locked/done=false，也可能对外报告done=true。真实异常handler反例先失败；修复后保留currentTruckNo和游标，锁释放后继续扫描，round_trucks只在完成时推进。
2. **异常写入达到120上限，剩余事项被当作完成。** 121条合成整车异常原只写120条却返回done=true。扫描现在记录待写事项，返回未完成及重试游标；下轮补第121条，再次调用无重复。旧异常解决也验证120+1续跑，最终才报完成。

两个反例位于`scripts/fillingConsistency.test.cjs`，调用真实crm-bottle-anomaly处理入口，仅使用受控存储替身。它们证明本地业务分支正确，不证明支付宝事务隔离、真实调度或现场设备已通过。

## 主任务实际验证

| 检查 | 结果 | 证据 |
|---|---|---|
| `npm test` | **118/118通过**；3个检查器、18个语法入口、19个测试文件 | `/private/tmp/crm-mainline-owner-acceptance-20260908.log` |
| `npm run build:web:cloud` | 干净源码bec921d构建通过 | `/private/tmp/crm-mainline-owner-build-20260908.log` |
| `checkReleaseIntegrity --product=web --require-clean` | 通过 | 最终代码提交后运行 |
| `checkReleaseIntegrity --product=cloud --require-clean` | 通过 | 最终代码提交后运行 |
| `git diff --check` | 通过 | 合并与文档更新检查 |
| `node scripts/checkProjectContext.cjs` | 通过；STATE 58行，原STATE归档完整 | 当前清单改查本交接；两份早期阶段交接保留按需链接，不再注册为当前交接 |

候选H5版本：`1788859881292-a1f61031`，生成时点`2026-09-08T09:31:21.292Z`；入口`/assets/index-kxqFmMzQ.js`。源码摘要`32c9e31793e765c7ca980b4e1a0cdd88b153f20a5d6e64a1aadd2192fb0b8833`，产物摘要`95c96f874c9e67be953c10a5adb2d7581e134d9954b84868e8db09ba356cf304`，sourceDirty=false。证据来自本机`dist/build/web/version.json`；构建配置写有目标空间不等于已部署。**这是包含未验收灌装入口的完整候选产物，不得直接发布。**

## 生产保护与下一步

- 生产记录仍为`101068bb9580b6c0faa1b9b83886789d8b5c2de3`；本轮没有发布，也没有重新读取线上运行文件。两个客户的原始修账批次已完成，不重建、不加收款。期初预付款识别和period_summary `.2`必须保留。
- **通用账务可独立推进。** 以101068b建立新的最小发布树，按T1交接的19个源码文件组合构建4个完整函数包及H5；保留函数本地辅助文件。先对当前历史分类完整范围进行只读影响核查，避免把旧时点38张当当前值；然后按源码、产物、运行版本及两客户保护反例完成发布验收。当前尚未生成该正式发布提交或执行上线。
- **T4已派发：PDA灌装完成与恢复协议。** Astra极高；冻结首次结束时间/称重/操作者及稳定操作号，恢复已保存源单关联，区分物理完成、源单保存与后续处理。工作区`/Users/wangbo/.codex/worktrees/0ea9/2026_v4`，已继承bec921d；不改设备目标/联锁语义、不自行发布。
- **T5已派发：灌装导入重试与保存确认。** Sol Ultra；稳定源操作及冻结内容、查询确认才计保存成功、恢复日志、预览零写入；不运行线上导入。创建回执与后续工作区统一在[主线总表](../../docs/SYSTEM_MAINLINE.md)维护。
- **灌装真实环境条件尚未满足。** 需要明确非生产支付宝空间、事务/索引/定时器及权限回执、监管隔离接收端、53瓶真实中断恢复演练、PDA安装与现场验收。生产真实瓶不作为未经安排的测试对象。PDA和导入两个任务完成也不能自动消除此门槛。

下一阶段验收由主任务负责；子任务报告保存到自身提交，主任务直接读取。销售多对象持久写入、历史改单恢复、全债权催收和汇总提速继续留在后续列表。四项真实成效尚无完整测量，本地118项通过不换算成线上积压为零或返工下降。

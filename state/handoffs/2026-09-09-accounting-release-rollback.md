# 通用账务发布尝试与已验证回退

更新时间：2026-09-09 15:32（UTC+8）。本交接替代[发布准备](2026-09-09-accounting-release-preparation.md)中的“等待停录”状态。**已执行发布，但新版本未通过最终验收；原生产已恢复，并已通知恢复录入。**

## 当前运行状态

- 目标：支付宝 `env-00jxuffegf2n`，空间 `xintuonengyuan`。
- 当前网页：源码 `101068bb9580b6c0faa1b9b83886789d8b5c2de3`，构建 `1788853708683-05ed825f`；原版完整179文件恢复上传成功，线上版本、入口与样式5文件逐一比对通过。
- 当前四函数：已恢复发布前官方实际下载的完整包；再次下载25个文件，文件集合及全部SHA-256与原备份逐一相同。保留原线上权限文件，不能只用Git基线重新打包代替。
- 候选 `5021753b6e03a938c371357c63ce0f533082a52c`、H5构建 `1788928538096-1d9dfc98` **当前不在生产生效**。源树、候选包和失败证据均保留。
- 没有数据库变更、账务修正、新收款/分配、余额批量刷新或催收重算。没有上传灌装、PDA原生、网关或数据库结构。

## 窗口与实际结果

用户确认“已暂停”于14:58:46被接收；等待超过线上60秒请求超时后取窗口原值。此等待不是服务端在途请求遥测。约15:31完成回退验收并通知恢复录入；私有状态落盘时刻15:31:58为记录时间，不能冒充更精确的现场恢复时间。

1. 依次上传结算、销售、首页、催收四个候选完整包；平台回执全部成功。实际下载33个文件，逐个与候选核对一致；对应只读业务冒烟通过。
2. 候选后端两家客户三个期间×对账/两种导出共18项通过；按重量、按瓶、代理、整车、两家流量、无业务七类客户共29次只读请求通过。原有3客户共47项旧内嵌收款缺到账日期继续一致待核，不属于新增分类失败。
3. 候选H5平台回执180/180文件上传成功。随后逐资源回读在 `assets/AppSkeleton-DWBgQW3-.css` 未通过，未生成“全部资源一致”回执。失败记录当时仅保存路径，不能据此认定具体HTTP状态、缓存原因或字节差异。
4. 按用户已批准的失败即回退规则，停止验收上线，恢复原版网页及四个原函数。原网页入口/版本检查通过；四函数25源码文件完整回读一致。
5. 回退后重新通过两家18项期间核验、代表场景29次只读检查。窗口起点、候选上线后、回退后三份原始账务逐字段及快照哈希一致。两家销售运营值、销售结清状态、流量、收款、分配、期初和凭证均不变。
6. 实际浏览器进入登录页，候选业务页面视觉验收未完成；不将API通过或上传成功当成页面验收。回退后入口正常呈现登录页。

代表检查第一次与另一核验脚本同时登录同一审核账号，随后收到登录失效；代码确认新登录覆盖旧token，改为顺序运行后完整29项通过。该工具协作问题与候选账务计算失败分开记录。下载首页源码第一次被本地沙盒的进程读取限制阻止；按权限规则重试官方工具后成功，未混同为线上部署失败。

## 保护结果

| 已确认范围 | 回退后结果 |
|---|---|
| 客户A，本年 | 营收86,708.803元、实际收款164,532.100元、历史回款77,823.297元，零欠款 |
| 客户A，跨年/历史月 | 跨年应收164,532.100元；2025年12月应收77,823.297元、实际收款0；与发布前一致 |
| 客户B，截至09-07 | 结算费用289,710元、实际收款280,000元、可用预付款36,361.68元、未结气款0 |
| 客户B，期初 | 46,071.68元仅为期初预付，跨年查询也不计新收款；11张有效流量、4笔实际收款不变 |

保护客户两份快照SHA-256：`d40dc0e5ad5ea66c18807bab800b01623f3509bb2e71137c1224952c1106734c`、`2283e5a4fd6e92e5fb2fb83c330aac7dc6e859409ccc1c6ee1e034812bf46344`。全部逐字段比较，不仅检查总金额。包含作废记录的集合数量不能当成有效笔数。以上不代表全体客户历史账已审计。

## 证据位置

原始资料仅存Git忽略的受控本地目录 `outputs/trust-audit/2026-09-09/accounting-release/`（目录0700，文件0600）。当前文档仅保存脱敏结论、摘要及位置。

- `window/window-status.json`、`release-status.json`：切换、恢复和当前版本；窗口已关闭，不再沿用此次停录授权自动重发。
- `window/upload-crm-*.log`、`window/readback-crm-*.json`：候选四函数上传回执及33文件比对；四个完整候选ZIP/哈希见准备交接和候选清单。
- `window/upload-h5.log`、`window/h5-readback-error.json`：候选网页上传及资源失败证据；候选H5产物摘要 `4333136900ac4392113e67f85c086dfb1c173ece14d078de8684656d02dd5bb6`。
- `window/rollback-h5.log`、`rollback-web-verified-*.json`：原网页恢复；产物摘要 `ea74e49966c86f325d1d0e9ab5ec5d2766415db47eccd62285ebdf7911ed8593`。只声明5个关键远端文件已核对，不声明全部旧懒加载资源已远端下载。
- `window/rollback-functions.json`、`window/rollback-upload-*.log`、`window/rollback-download-*.log`：原函数恢复与25文件逐字节证明。
- `window/original-comparison.json`：窗口前后及回退后三份原始账务逐字段完全相同；18项保护核验各有完整响应。
- 原始窗口起点 `1788937244802`；候选后端保护核验 `1788938597749`；候选代表核验 `1788938678192`；回退保护核验 `1788939004985`；回退代表核验 `1788939054883`。代表证据位于 `raw-export-1439/live-*-verification.json`。
- `window/resource-failure-audit/`：Sol Ultra受限只读复核；禁止再次上传或调用账户登录、业务写入。

## 窗口外的检查工具补强

主工作区单独提交 `bb45947`：现有发布校验分别保留HTTP失败、请求/响应体读取失败、版本内容错误、版本字段不符和内容哈希不符；记录已完成文件的状态、字节数与SHA-256。错误回执不保存响应正文，不自动重试或放行差异。`scripts/releaseEvidence.test.cjs` 12项通过，含本次证据不足的反例。它是检查工具改动，未发布，不更改候选5021753或业务运行代码。后续不能再用仅保留错误路径的临时核验器替代已验证入口。

## 后续只读取证与受限差异清单

Sol独立探测排除本地空文件/损坏和当前固定404：526字节的样式原文实际收到528字节，仅多前置CRLF。该文件在候选与原版本中相同。原失败日志无法区分当时HTTP与哈希失败，因此不能倒推其当时具体状态，也不能准确归因到源站、CDN或网络中间层。

主任务另用原核验器同样的Node `fetch.arrayBuffer` 交叉验证：当前index.html及入口大样式字节完全相同，失败样式仍只多CRLF。curl/identity对部分HTML和CSS也观察到CRLF，交付表示有请求路径差异；不把curl的HTML现象泛化为浏览器或Node必然返回同样内容。

对候选177个非根HTML资源做窗口外只读核查：162字节完全相同、15个CSS仅多一个CRLF、其他差异0。根HTML、版本和清单当前已经回退，不纳入该候选资源核查，不能把它叫做候选H5全体验收。证据为私有 `window/post-rollback-candidate-assets-audit.json` 与 `window/direct-resource-comparison.json`。

检查工具另增加显式[15项样式交付清单](../baselines/2026-09-09-web-css-delivery.json)：绑定候选完整提交、buildId、每个原文SHA-256和实收SHA-256。默认仍按原字节严格检查；只有明确传入该清单、路径是对应CSS、内容类型正确、响应恰好为单一CRLF加完整原文时，才识别为已声明交付表示。禁止BOM/@charset与根页面SRI；JS/HTML不适用，重复空行、版本变化、路径变化和任何内容修改仍拒绝。回执同时保留原文及实收两种哈希，不谎称原始响应字节完全相同。14项相关检查通过（含正式只读入口的清单加载、错误哈希拒绝及回执保全）；业务代码和候选产物5021753未改、未重建、未再上传。

下次使用已提交的正式只读入口 `scripts/verifyWebReadback.cjs`，不依赖临时脚本。检查工具及固定清单提交 `2fa23e5`；默认严格字节，显式策略要求调用方给出清单SHA-256。策略按JSON对象序列化的摘要为 `ec68ae960d1c3a20d2203d276e3b270fde6e59518f70a0e2a8309014c6fddd1d`。入口先检查完整本地产物摘要、版本和支付宝环境，失败/成功均保存回执且禁止覆盖已有回执。此次增加UTF-16/UTF-32 BOM字节检查，覆盖独立审查指出的遗漏。

下一次H5上传后从主工作区执行以下只读验收；每次使用新的回执文件名。该工具不构建、不上传、不登录，候选源码和产物仍是5021753。当前原版本会按版本不符拒绝，不可把窗口外的资源抽查代替上线后的此项完整验收。

```sh
node scripts/verifyWebReadback.cjs --directory=/private/tmp/crm-accounting-release-20260909/dist/build/web --receipt=outputs/trust-audit/2026-09-09/accounting-release/next-release-web-verification.json --policy=state/baselines/2026-09-09-web-css-delivery.json --policy-sha256=ec68ae960d1c3a20d2203d276e3b270fde6e59518f70a0e2a8309014c6fddd1d
```

默认 `releaseWeb.cjs` 不隐式加载此差异清单；正式只读入口及其测试已经进入Git，独立候选目录未改。实际发布须仍使用独立候选，不从主工作区构建全量H5。原临时 `window/verify-accounting-next-web.cjs` 只保留过程原文，不作为下次正式入口。

## 下一步

停录窗口已经结束。资源范围及检查工具补丁已准备；下一次需新的实际停录确认，并重取期间变化。当前浏览器经只读观察仍在登录页。先把API核验按顺序完成，再请现场登录网页做最终页面验收；此后不再由API脚本登录覆盖该页面会话。运行只读验收不得并发登录同一账号；把全部待用上传/核验操作准备成有失败即停的顺序流程，缩短停录时间。不自动重传生产，不混入全量候选。当前A-08/A-09通用候选仍未完成上线交付，A-06/A-07前置期间汇总及期初支持继续在原版本生效。灌装独立验收安排不变。

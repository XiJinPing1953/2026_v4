# 出纳金额小数点输入修复发布（2026-09-24）

## 范围与版本

- 用户确认三个金额框的 H5 输入方式修复，并授权发布正式环境。
- 基于上一正式 H5 源码 `15f780c` 建立隔离分支 `codex/cashier-decimal-input-fix`；输入修复提交 `a757a44`，独立网页发布范围提交 `f594c1e`。隔离工作区：`/Users/wangbo/.codex/worktrees/cashier-decimal-release/2026_v4`。
- H5 正式 buildId `1790221263381-e73255b8`，源码 `f594c1ed36ea52751aac4550fef88d4679603637`，产物摘要 `b3052123d4f9d6975b09a2df4b22ad612ca07c64c22a043bc55cc658fe6c22d2`。目标为支付宝云空间 `env-00jxuffegf2n`、域名 `https://xintuonengyuan.com`。
- H5 的“到账总额”“其中气款”“其中押金”改为文本输入并提示小数键盘，绕开 uni-app H5 `digit` 转为 HTML `number` 后对小数点的缓存处理；支付宝小程序仍用 `digit`。现有金额精度、拆分核对、预览和保存规则未改。未上传云函数或 schema，未写入业务数据。

## 验证与发布

- 27 项出纳前端测试、5 项发布范围测试、H5 与支付宝小程序构建通过。独立发布范围检查显示 `sourceDirty=false`；正式 H5 包与源码提交绑定。
- 首次调用 HBuilderX `hosting deploy --prj 2026_v4` 时，CLI 按已打开项目名取到了原工作目录的旧构建包，并上传了旧版本 `1790044811996-17160025`。版本回读立即发现；随后把隔离候选包按 181 项产物摘要原样暂存到该 CLI 实际读取目录，重新上传 183 个文件，上传结束返回 `hosting deploy:OK`。原工作目录的旧构建包已恢复到原位置。后续隔离工作区发布不得仅依赖同名 `--prj` 解析；需在上传前核对 CLI 实际读取路径。
- 最终正式域名与云托管地址的 `version.json` 均返回 `1790221263381-e73255b8`。182 项版本及静态资源回读通过；15 项 CSS 按已声明的前导 CRLF 分发差异核对，其余按原始哈希核对。回读记录标记财务写入为 0。
- 已登录 Chrome 的正式出纳页逐键输入 `12.34`，输入框及“到账总额”提示均显示 `12.34`；未选择客户、凭证或提交收款。点击“重置金额”后提示恢复“待填写”。这是一次网页交互核验，不能替代不同手机键盘的现场复验。

## 证据与回退

- 构建版本、候选清单、CSS 差异策略和完整资源回读在 `outputs/cashier-decimal-20260924/`。发布源码保存在上述隔离分支，原工作区其他未提交改动未进入候选包。
- 上一正式 H5 包 `1790219575288-063d4ce1` 保存在 `/Users/wangbo/.codex/worktrees/inspection-due-release/2026_v4/dist/build/web/`；回退后仍须核对正式域名版本及资源。

# 客户对账工作区试做 · 2026-09-20

## 范围与保护

用户批准本地试做，不发布生产。分支 `codex/statement-workspace-preview`，独立工作区 `tmp/statement-workspace-preview`；回退基线为已推送 main `1ed1ec5b5612fae02e3c116f9f231d88807d04c7`。

原主工作区未提交资料已保存至主工作区 `outputs/trust-audit/2026-09-20/statement-workspace-backup/`：manifest.json、tracked.patch、uncommitted-files.tar.gz（57 文件，16,454,357 字节，逐文件 SHA-256）。没有带入试做提交。

## 已实现

- 总览后独立工作区，默认账务操作；紧凑固定导航显示客户、期间、净欠款。净欠款打开欠款来源销售视图。
- 可用内容宽度至少1100px时收款表单与分配依据并排，窄屏上下。期间依据先用原全量汇总，具体单据来自原预览接口。
- 勾选目标和可编辑预览10条/页，跨页保留选择和编辑；销售10条/页（近100条），流水保持服务端分页10条；收款单5条/页；流量历史5条/页（近20条）。
- 桌面销售/流水紧凑表格，手机卡片；详情保留长备注、瓶号和原单据操作。
- 工作区使用展示切换保留草稿、页码和整单调整；切客户重置。金额、日期、方式及选择变化立即失效旧预览；迟到响应不会恢复旧预览；确认处理器也拒绝失效预览。
- 本页小额抹零仅取当前销售页；其他合计及分配校验继续使用完整输入。未修改公共服务、后端、数据库及权限。

## 验证

- H5 构建通过；正常构建产物不含模拟客户、模拟登录或预览日志标记。
- 相关前端与模拟回归24项通过：分页边界、跨页选择/编辑、失效及迟到预览、确认拦截、本页抹零、会计/押金已有保护、模拟收款/冲抵/整单调整取消恢复。
- 浏览器本地模拟：工作区切换保留金额；跨页选两笔合计700；第三页仅3笔；修改金额显示预览失效；模拟确认入账使收款增加700；收款单5条与调整入口可达；整单调整切换工作区后保留、可取消；流量末页2条；切换客户清空草稿。
- 桌面1440、窄屏1000、手机390像素检查。桌面收款双列，1000单列；390无横向溢出，手机销售为卡片。截图见 `preview/statement/screenshots/`。uni-app 日期组件在开发热更新并连续跨桌面/手机尺寸时可能需刷新，截图与手机验收使用对应尺寸首次加载。
- 模拟仅测试界面和写入路径，不代表实际云端账务核算验收；未执行线上写入。

## 交付与继续

运行 `npm run preview:statement`，访问 http://127.0.0.1:5190/#/pages/customer/statement 。详情与模拟限制见 [预览说明](../../preview/statement/README.md)。页面提供 kg、瓶、m³ 示例和重置按钮。

等待用户体验反馈。满意后再决定生产发布；不满意可继续改试做分支或放弃，main 和线上版本保持原样，无需回滚生产数据库。

## 用户反馈与上线准备

用户随后授权正式上线并保留回退。已修正：分配依据上沿与输入框上沿、左沿与第三张汇总卡左沿精确对齐（1440px实测偏差均0）；销售表格增加单号摘要、业务/付款状态标签、金额右对齐、交替行底色；标题与按钮在窄屏分行。620/390px标题正常横排，页面无横向溢出。相关24项回归再次通过。

正式发布仅H5。主工作区原H5 `1789832959489-f58761c2` 已与线上版本及产物摘要核对，并完整备份至主工作区 `outputs/trust-audit/2026-09-20/statement-workspace/rollback-web`；旧版CSS交付策略及回读证据同目录保留。界面反馈截图位于同目录。待发布后补运行版本和回读结果。

## 正式发布结果

用户明确批准上线后，已发布支付宝 `env-00jxuffegf2n` H5：`1789836236314-37224524`，源码 `c18928abf1c21c6ec5796f7a1fee45fc80ccc4ee`，产物 SHA-256 `09d6261ff59cc36269c0e18c1eaa4f6adc3b894d9b9921b319a64195e174a4f4`。181文件上传成功；180项资源回读通过（15项CSS为绑定本构建双哈希的已知单次CRLF前缀），证据为主工作区 `outputs/trust-audit/2026-09-20/statement-workspace/web-readback.json`。24项前端/模拟回归、90项账务/发布回归以及正式构建/源码空间清单检查通过。

正式站登录页正常；本次验证浏览器无生产登录态，未复验登录后的真实客户页面，也未写入真实账务。模拟交互验收与布局证据见上文。云函数、数据库和权限未部署变更。

回退：在主工作区运行 `HBuilderX cli hosting deploy --prj 2026_v4 --space env-00jxuffegf2n --provider alipay --source outputs/trust-audit/2026-09-20/statement-workspace/rollback-web`（CLI全路径同 scripts/releaseWeb.cjs），再用该目录绑定的旧CSS策略回读验证。将恢复 `1789832959489-f58761c2`。

## 操作页签跳动修复

用户反馈页签切换跳动。修复不同操作按钮组造成的头部高度变化、短表单导致的滚动位置收缩，固定页签字重防止选中后挤动；重复点击当前页签直接忽略，不触发调整取消。未改账务校验与提交逻辑。1440/620/390px连续切换收款、退款、收款单、押金、预付，页签Y坐标和scrollY均保持不变。工作区8项回归通过。

修复已发布H5 `1789836925317-c58040cf`（源码 `8be51e1`），181文件上传、180资源回读通过。证据为主工作区 `outputs/trust-audit/2026-09-20/statement-tab-stability/`，含tab-positions.json、web-readback.json和本构建CSS策略。仅H5，原回退包保留。

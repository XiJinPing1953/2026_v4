# 天然气库存查询超时修复

日期：2026-09-19。用户要求修复库存页 `[crm-gas-in]: 函数执行超时`。

## 已确认

- 支付宝云空间 `env-00jxuffegf2n`，真实 `listV1` 请求返回 HTTP 500 / `50008`：`functionRequestTimeout 60000ms`，服务端耗时 `1m0.000852189s`。不是推测客户端等待超时。
- 列表原来串联全范围入库汇总、历史库存流水扫描和当前库存重算。当前库存再扫描账期灌装、按瓶组分页查历史流转、扫描账面流水。

## 最终修复

- 最终源码 `e6ad3ea2201a`（初始实现 `00d0c4f`，支付宝兼容修正 `c5cda50` / `e6ad3ea`）；新页面列表和导出传 `include_inventory:false`，库存独立请求，失败不阻塞入库列表。旧调用者默认仍保留原响应字段。
- 最新流转采用基础查询，每组300个瓶号、每页1000条；已找到最新记录的瓶号从后续查询移除，不因缺失瓶号继续扫描已解析瓶的全部历史。保留事件时间、类型、创建时间排序，并补 `_id` 确定性排序。旧版支付宝数据库不支持本次尝试的排序后 first 聚合，已移除该实现；账面 sum 聚合线上通过。
- 账面库存由数据库聚合总和，与其他库存读取并行；失败返回不可用，不回退为期初库存。灌装数据仍完整分页读取，超限明确失败，不截断计算。
- 前端库存失败/未加载显示 `--`，失败重试及列表/库存响应顺序不会覆盖已加载库存。保持30秒自动刷新。
- 上传包内包含由 `scripts/syncGasInventoryRuntime.cjs` 生成的储罐公共源码副本，领域副本检查覆盖；权限规则保持原版。函数执行配置明确为60秒。
- 验证：8项测试通过（含原库存规则测试文件、大量历史、601只瓶跨批、12万条以上账面合计、缺失/失败、前端响应顺序与恢复），H5云空间构建、web/cloud发布依赖及源码干净检查通过。

## 发布与验收

- 用户恢复HBuilderX登录后，`crm-gas-in`最终源码 `e6ad3ea2201a`上传成功；仅该函数，无schema或业务数据变更。
- H5 `1789813847569-5574f7a1`，源码 `e6ad3ea2201a`；179/179文件上传成功，179项版本/资源回读通过（包括已知CSS CRLF交付变体）。
- 生产只读探针：轻列表538ms，58条入库记录；当前库存8808ms，359只候选瓶完整核对，已灌未售49只/3.205吨、待核0、账面储罐9.034吨。现场储罐6.85吨、总库存10.055吨是当次时点值，后续遥测会变化。
- 原始请求已复现60秒服务器超时；新请求无读取错误，不通过截断历史数据或写库存值实现提速。
- 已登录生产新版本并完成DOM与截图验收：本月入库3条/61.7吨/377733元，现场库存可用，已灌未售49只/3.205吨，账面9.034吨；原超时提示消失。页面初次加载库存显示`--`，加载完成后正常显示结果。
- 证据与完整候选包：`outputs/trust-audit/2026-09-19/gas-in-timeout/`；`after-listV1.json`、`after-getCurrentInventoryV1.json`、`web-readback-network.json`、`cloud-upload.log`、`web-upload.log`及`cloud-accepted`、`web-accepted`。
- `cloud-before.tar`为已跟踪本地旧函数基线，未取得线上源码回读，不能称为生产原包。`web-before`保存上次生产入口及版本；不宣称包含旧版全部静态资源。
- 初始两个云端聚合候选验收未通过，已由最终基础查询版本替换；不把前两次结果作为成功证据。

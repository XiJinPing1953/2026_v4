# 流转异常刷新续扫与待处理复核（2026-09-24）

## 交付与发布边界

- 以 `main` 的 `ef95a7c` 为起点，在 `codex/anomaly-refresh-completion` 提取并实现持久任务、单瓶扫描锁、完整读取核验、定时恢复与管理员重试；最终云函数源码提交 `7496a96`。保留正式云端原有异常封存和超级管理员权限逻辑。未合入 `codex/system-trust-foundation` 的灌装/PDA候选。
- 正式支付宝云空间 `env-00jxuffegf2n` 上传 `crm-bottle-anomaly`、`crm-filling`、`crm-sale` 三份函数；创建私有集合 `crm_anomaly_refresh_jobs`、`crm_bottle_scan_locks`，并在控制台增量建立和回读 `idx_status_retry_lease(status ASC varchar, next_retry_at ASC double, lease_until ASC double)` 非唯一索引。函数为 Node 18、60 秒、512 MB；一分钟定时器 `bottle_anomaly_refresh_resume` 已在控制台回读。未发布 H5、既存异常/流转 schema，也未初始化数据库。
- `touchV2` 在扫描前持久入队，返回 `job_id/status/pending_total`；只有整批完成才返回 `done=true`。单瓶跨轮游标含唯一记录编号，读取不完整、查询失败、取锁失败或历史摘要变化时不关闭旧异常。灌装/销售入口区分已入队后台处理与入队失败。

## 线上验收

- 发布前全量异常接口快照共 218 条，其中待处理 23 条；逐瓶完整分页读取流转，预览预计关闭 19、保留 4、待核 0。按用户选择先在正式空间验收 238 号瓶：回瓶 -2 kg、灌装 171 kg、出瓶 169 kg；读取 66 条流转后，旧缺灌装提示由 `system-reconcile` 关闭。
- 单瓶首次暴露支付宝云数据库对象字段读回顺序不同，目标摘要错误；已改为固定字段顺序计算并加测试。发布切换期间旧实例仍处理到任务，22 瓶任务首轮处理 19 瓶后于 `2026-09-24T07:48:23Z` 被旧摘要算法标记失败。管理员重试入口在新版本上恢复并完成 22/22；同时改为摘要失败也经过最多五次退避，避免一次旧实例调用永久停单。此过渡失败不计作自动恢复成功。
- 独立 34 目标验证：22 个原瓶号加 12 个预先确认无流转的哨兵编号。`2026-09-24T07:56:15Z` 首轮完成 28/34，`done=false`、剩余 6；`07:56:23Z` 定时器自动处理至 34/34，`done=true`、剩余 0、错误空，未调用人工扫描或重试。这验证关闭页面后的服务端续扫。
- 最终逐条回读：原 23 条待处理关闭 19、保留 4、待核与未完成均 0；仍待处理瓶号为 `246、291、315、2`。238、242、243 已关闭；240 在本批前已关闭且复核仍非待处理。全表前后均 218 条，无新增或丢失；19 条只变更 `status/updated_at/resolved_by_name`，4 条真实异常仍为 open、仅刷新 `updated_at`。22 瓶的 2401 条流转接口记录与复核前快照逐条相同；240 号瓶的独立快照亦相同。未补灌装、记损耗或删除源单。

## 验证、证据与回退

- 本地任务、锁、幂等、退避、权限、同时间记录分页、单瓶跨轮、扫描中改单及 240 缺灌装反例测试均通过；云发布依赖检查在干净工作区通过。云端验证了集合命名上限、任务条件租约更新、唯一主键去重、索引、管理员重试和真实定时触发。单瓶首次故障与发布切换期间旧实例事件均保留为验收证据。
- 受控本地目录 `outputs/trust-audit/2026-09-24-anomaly-refresh/` 保存前后异常接口快照、22 瓶流转预览与最终逐条比较、异常逐条结果、任务回执、34 目标定时回执、发布清单及发布前三函数回退包；目录权限 700、文件 600，不入 Git。发布前函数回退包 SHA-256 为 `76e44c9068849fed5a9e10b6ef3fbcd0b67b5305d099872c47968600d7d66ffc`。接口快照不是数据库原生备份。
- 如需回退，先暂停该函数的定时触发，再从 `production-functions-before.tgz` 恢复原三函数；保留新任务/锁集合、任务进度及异常前后快照，不删除或反向改写流转源单。任何异常状态反向变更需逐条对照前后证据后执行。回退时不要上传本地既存异常/流转 schema 覆盖线上结构。

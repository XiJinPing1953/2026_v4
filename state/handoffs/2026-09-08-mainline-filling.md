# 主线灌装保存与异常闭环交接

2026-09-08；主任务 `01a071ca-9c43-7df3-97b4-96f1e0870ab0`。交付是工程补丁及隔离验收材料，**未部署、未进行真实云端验收、未访问或改写生产钢瓶、未安装 PDA**。当前工位 PDA 后台存在明确协议阻塞，不能将本交付直接标记为生产可发布。

## 基线、提交与归属

- 独立工作区 `/Users/wangbo/.codex/worktrees/0f60/2026_v4` 初始干净、分离在 `90336801cf67d7c31b9df7104de3c1545452285f`，已验证是指定主线祖先后建立 `codex/filling-consistency-20260908`。
- 工作基线 `d9eb4fb9dc8edff82789a86a89631f4f86f70b20`；生产已发布小范围源码由主任务确认是 `101068bb9580b6c0faa1b9b83886789d8b5c2de3`。本补丁依赖主线已有的灌装持久操作候选，不能单独叠到缺这些模块的生产树。
- 工程提交 **`682915938e305072c6e68b33a2905d6528f6a67a`**，14 个明确范围文件。后续文档提交记录于本分支历史；主任务可先审查工程提交，再连同文档合并。
- 根 `STATE.md`、`AGENTS.md`、`docs/RULES.md`、`state/INDEX.md` 未修改。账务、发布脚本、网关、主工作区和 `/private/tmp/crm-julite-release` 未修改；不含客户原始数据、凭据和日志。依赖从原工作区只读克隆到本独立工作区后构建，没有运行依赖安装或修改原目录。

## 结果、关系与最小失败路径

完整接口契约和发布顺序见 [灌装一致性说明](../../docs/filling_consistency_v1.md)。

| 问题 / 最小反例 | 修补及对应文件 | 本地证据 |
|---|---|---|
| 源单事务已提交，保存检查点确认丢失；事务 doc.get 返回对象时，旧代码取 `[0]` 得不到源单，重试失败 | `crm-filling/index.js` 兼容事务对象/数组/空值，保留确定源单 ID 与原操作 | 将模拟事务改为官方文档的对象返回后原测试由通过变为失败，修补后恢复通过 |
| 源单已在库，`saved_cursor` 仍为 0，用户无法判断是否保存 | `fillingOperations.js` 的 `getOperationV1` 按冻结 ID 查询 `source_records/source_saved_total`；状态面板可查看 | 确认丢失反例查到 1 个源单，并通过真实 handler 的 `getV1` 查回；不泄露 worker_secret/token |
| 单条监管返回 code 0 但事件 0 条，旧代码仍完成 | 灌装端校验事件和快照入队/去重计数，固定源类型 filling，失败保持已保存和可重试 | 0 入队时未完成；更新账号凭据后用新凭据重试成功；凭据不存操作 |
| 扫描期间源版本变为 2，旧扫描返回 done；或条件完成更新输掉版本竞争 | 扫描完成后再次读取版本，条件更新未命中回读；缺完整读取或规则版本不符也不完成 | 两种竞态均停止旧操作，保留新源单重量；不完整扫描不推进目标计数 |
| 1,001 条旧事件缺 type_order/created_at，旧游标到第 200 条后跳过余下事件 | `crm-bottle-anomaly` 先完整读取和内容见证，再规范化内存分页；普通扫描和持久扫描共用；续扫保留 `_id` | 原反例只读 200 条即结束；修补后 800 + 201、零误报 |
| 53 瓶处理完 6 个后函数中断 | 继承并重新验证已有持久任务、瓶号位置和瓶内游标 | 灌装真实 handler + 模拟扫描/数据库恢复剩余 47；源单/充装/库存各 53，原编号重试无新增 |
| 5,189 条历史输入可能被查询上限截断或误判 | 异常完整读取及原有共享规则；删除已完整取数后仍按 5,000 数量强行保持未完成的旧条件 | **实际异常 handler** 读取 53 合成瓶的全部 5,189 条有效事件，生成 0 条异常；不是只测分页工具 |
| 手动重扫与后台核查同瓶并发 | 普通/整车/touch/rebuild 扫描共用 scan 租约；灌装状态仍独立 projection 租约 | 已持有后台租约时手动扫描返回等待、不写异常 |
| 事务接近云端时限 | 提交前达到 8 秒则回滚；操作记录最近成功事务耗时 | 模拟 8.1 秒事务回滚无孤立流转，延后重试成功；不代表真实 10 秒时限已测 |
| H5 受理被显示成已保存；本地未确认编号累计、旧任务协议被新后台误执行 | 单条提示区分完成；面板列编号/源单；查询本地遗漏操作并释放完成编号；新前端与任务执行核对 v2 | 超时沿用编号、旧后台拒绝、完成草稿释放、未确认草稿保留、旧协议任务不执行 |

当前协议 `filling-consistency-2026-09-08-v2`。`common/bottleFlowRules.js` 及三份副本未改，规则仍为 `bottle-flow-2026-09-05-v1`，副本检查通过。三个函数依赖既存的独立目录规则/ACL/完整读取副本；本轮给异常和流转函数补上明确的 60 秒配置，灌装原有 60 秒/120 秒租约关系保留。

## 已执行验证

2026-09-08 本独立工作区实际执行：

- `node --test scripts/fillingConsistency.test.cjs scripts/crmFillingMaintenance.test.cjs scripts/bottleAnomalyArchiveCutoff.test.cjs scripts/bottleAnomalyMissingFillPermission.test.cjs scripts/frontendTrust.test.cjs`：**45/45 通过**。其中 53→6+47 与 5,189 完整历史是两个不同入口的反例；不把它们写成已跑过真实云端端到端用例。`frontendTrust` 内原有两项销售只读/展示测试也随文件运行，未修改账务实现或这些测试内容。
- `node scripts/syncDomainContracts.cjs`：通过，同步 0 项；三个相关 JS 入口/工具语法检查和 `git diff --check` 通过。
- `npm run build:h5`：最终修改后构建通过。依赖只有版本提示和旧 Browserslist 数据提示；未更新依赖。未进行本轮浏览器视觉验收、支付宝小程序构建或 PDA 真机安装。
- 本地 H5 检查产物：版本标记 `1788858031130-7553f269`、179 个文件；按相对路径排序，以 `路径 + NUL + 内容 + NUL` 拼接计算 SHA-256 为 `69a56d2d2620541d38f32de3571c3d6ee36f5f68bb72f7f026c671ebf5e4115a`。该产物构建时记录 `source=d9eb4fb9dc8e dirty=true`，**仅作编译证据，不得发布**；主任务须从最终已提交发布树重新构建。
- `node scripts/fillingCloudAcceptance.cjs --run 20260908A`：只执行了离线预演。合成对象摘要 `e5fb603cdd9dbe34a3c7055c5c24a89180e278f0bb837afab674f2646486f380`；本轮没有加载真实云 adapter。`node scripts/checkProjectContext.cjs` 已通过：17 份当前资料、STATE 54 行、1 份归档完整。

模拟数据库串行事务不能证明支付宝隔离级别、回滚和冲突；mock timing context 不能证明平台真实触发来源；mock 监管返回不能证明 outbox 或监管平台已收到。

## 发布阻塞：当前 PDA 工位链路

实读代码链路：

`src/pages/pda/filling-create.vue` → `PdaFillingTaskCreateView.vue` → `src/services/pda/fillingTask.js` → `crm-pda-filling.createTaskV1`；完成页走同服务的 `completeTaskV1` → `crm-pda-filling.completeTaskInternal` → `callFillingCreate` → `crm-filling.createV1`。

范围外 `crm-pda-filling/index.js` 的当前事实：`callFillingCreate` 在约 426 行传原 payload，没有 operation_id；`completeTaskInternal` 在约 449 行开始，每次取最新秤值并生成新的 ended_at；约 503 行调用灌装后，只要 code 0 就写 task completed，不检查源单是否保存/操作是否 complete。因此只更新前端 `fillingOperations.js` 或给请求临时加随机号都不能闭环。该文件本批按归属要求保持未修改。

主任务下一补丁应明确包含：

1. 按 task_id 派生稳定 completion operation_id；第一次完成时先持久化冻结的最终重量、时间、操作者及 payload，后续确认丢失只查询/重放这份内容，不重新采秤或生成结束时间。并发完成须只有一个冻结版本获胜。
2. 区分物理任务状态、源单保存确认和后台核查状态；没有源单 ID/保存证据时不能写“记录已保存”。查询既有 operation 可恢复任务链接，已保存但核查失败时仍显示原单和剩余处理。
3. PDA 完成页展示编号、保存/核查进度和重试入口；标记异常入口走相同冻结协议。补任务后台反例（双击、秤值在重试时变化、源单提交后任务更新失败、code 0 未保存）并加入部署清单。
4. 清点旧 PDA 在途任务和运行版本，再统一切换新任务后台、H5/PDA 资源；保持现场工位控制流程由主任务协调。此工程窗口未改目标写入、网关、联锁或安装包。

## 最小云依赖与真实验收安排

发布源码由主任务重新组合，保留已上线的 period_summary/opening_prepay；不执行历史客户修账，也不把全量候选直接上传。

| 顺序 | 对象 / 条件 | 必须收集的真实证据 |
|---|---|---|
| 1 | 明确非生产支付宝测试 space-id、一次性合成对象、快照、测试账号；监管发送禁用或接收端隔离 | provider/space/运行配置回执，导入前精确 ID 不存在，恢复方案可执行 |
| 2 | 根目录 operations/slots/scan_locks 私有 schema；源单、瓶流转、库存流转版本字段；operations 的三个索引及瓶事件游标/源单查重/source_id 索引 | 控制台索引就绪；客户端 CRUD 全拒绝；确定 `_id` 冲突只允许一个写入；不覆盖未知线上 schema |
| 3 | 完整打包 crm-bottle-movement、crm-bottle-anomaly、crm-filling；三者超时 60 秒，锁 120 秒；既存监管桥和 outbox 唯一索引 | 依赖文件及源码哈希，能力接口 v2、流转规则版本相同；真实事务内 doc.get 返回形状、历史 where 读取和单记录回滚 |
| 4 | 灌装 trigger 包内七位 `17 * * * * * *` / 支付宝控制台六位 `17 * * * * *` | 平台真实 SOURCE=timing、每分钟触发、伪造客户端事件被拒绝；超时崩溃后锁过期可继续 |
| 5 | 合成 53 瓶与 5,189 条历史；每源单/流转/库存预期 53 | 测试专用受控故障注入在第 6 个扫描检查点后中止，保存操作状态；真实定时器恢复 47；同/不同操作号并发重试、源版本升到 2 后旧任务停止 |
| 6 | 单条和修补后的当前 PDA task 完成；普通用户/其他用户/管理员权限 | 监管事件+快照各 1 个，确认丢失后按 source/idempotency key 无重复；凭据失效可见失败；原始秤值不因重试改变；原生扫码/称重/连续瓶真机证据 |
| 7 | 有已提交源码的 H5/PDA 配套版本，已清点在途任务 | 暂停旧新建入口并完成切换；能力探测、页面保存/剩余状态和现场确认；只从最终提交重新构建并发布 |

故障注入条件本轮没有安装。不要向生产函数加客户端可控的“第 6 个停止”或“绕过权限”开关；在隔离空间使用调试器/测试专用包或受控云调用代理，记录测试代码哈希，随后恢复正式候选并复核。仅手改 operation 游标不能证明真实中断恢复。

只读工具的 adapter 契约、运行示例和精确恢复范围见 [说明](../../docs/filling_consistency_v1.md#默认只读的真实云验收工具) 与 `scripts/fillingCloudAcceptance.cjs`。它没有业务写入或部署命令，也不自动读取本机云凭据。尚未提供已验证的隔离空间、真实 SDK adapter、测试账号权限、云端索引/触发器回执、监管测试端和可安装 PDA，故本轮对应结果均为**未验证**。

## 并发和覆盖边界

同瓶扫描已统一租约；只有灌装模块的状态投影使用 projection 锁。销售、手动钢瓶状态、历史改单/导入及其他源单写者没有加入同一个提交协议，事务历史查询无法替代这些写者的协调。云端演练需记录哪些写者被暂停/隔离；不能把锁扩大到整个系统或宣称全局强一致。

完整见证是钢瓶流转集合的两次读，不是跨集合原子快照。异常逐轮写入，未完成/历史变化期间可能保留中间异常；持续并发可能导致重扫。整车销售/补气、已完成历史改单、销售多对象提交均未扩展到本轮持久任务。53 瓶及 5,189 条为合成反例，不能据此推算生产未完成任务数为零。

**下一条可执行动作：**主任务审查 `6829159`，接管并修补 `crm-pda-filling.completeTaskInternal` 的冻结提交/任务回链，再在明确的隔离支付宝空间先上传最小 schema/index 和三个函数，运行离线方案并安排上述真实演练；通过后才组合最终 H5/PDA 发布。无需用户转述或再次批准本批已授权工作。

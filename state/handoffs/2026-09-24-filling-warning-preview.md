# 灌装预警完整读取与批量预览（2026-09-24）

## 修复与发布

- 用户明确授权修复误报、精简批量预览并发布。从 `main` 的 `0f68637` 隔离实现，源码 `bcd22baea8bb`；未合入未发布灌装/PDA/导入候选。
- 正式支付宝空间 `env-00jxuffegf2n` 仅上传 `crm-filling`；五个函数文件下载回读与候选逐字节一致。H5 buildId `1790240785296-d4810f38`，182 项版本及静态资源回读通过，自定义域名版本一致；15 项 CSS 仅有已声明的 CRLF 前缀分发差异。未部署 schema，未新增、修改灌装、销售或回瓶业务记录。
- 预警及保存后的钢瓶状态同步共用完整游标读取，复用规范 `common/financialRead.js`，独立部署副本已加入同步契约。计数缺失、短页截断、查询失败、成员变化或读取期间更新均返回 `BOTTLE_FLOW_HISTORY_INCOMPLETE`、`read_complete=false`、`confirmable=false`，忽略软预警不能绕过。
- 同日流转与重量计算规则保持；预览异常优先再截取前 50 条，保留原行号。桌面重量分列，窄屏分行；回瓶依据与来源单号可展开。缺失数值显示“—”。预览失败清除旧结果，不完整状态持久显示。
- 如果源单已经保存而后续状态核查失败，响应明确 `source_saved=true` 并提示勿重复提交，不用不完整历史更新瓶状态。该补丁不引入未发布候选中的持久灌装任务，也不宣称保存后全部派生处理具备自动恢复。

## 根因与实测

- 旧查询对同批瓶的历史正序 `.limit(5000)`，重量依据另查。完整历史的后段回瓶/出瓶可能被截断，既会误报也会漏报。
- 先读取截图可辨认的 43 瓶共 4,916 条流转，再补读 10 瓶，组成 53 瓶共 5,736 条的独立反例；不是声称还原截图全部原始输入。接口快照受接口可见范围约束，不冒充原生数据库备份。
- 2026-09-24 同一 53 瓶旧线上预览 25 条预警；本地完整重放与修复后线上均为 36 条。旧结果有 10 条误报（358、252、228、93、198、271、390、62、122、315）和 21 条漏报；按完整数据截取前 5,000 条重放，恢复旧结果 25 条。此计数仅适用于取证时点和该批次。
- 192、358、228 的 08-31 回瓶同时存在于流转与两张销售源单，未发现这三个样本缺少对应回瓶。09-24 单瓶验收：358、228 无预警，192、59 保留真实出瓶预警。不能把历史已回瓶等同于此后一直在站。
- 修复后线上 53 瓶预览 `read_complete=true`，耗时 1,912ms，逐瓶预警集合与完整重放一致；三瓶总重预览通过。未点击执行或提交测试灌装。

## 验证与证据

- 12 项真实灌装 handler 反例及 17 项共享读取相关测试，共 29 项通过；覆盖 5,189 条末页回瓶、单瓶/批量一致、历史截止、后续出瓶、同日歧义、编辑排除自身、忽略预警不能绕过缺数、提交重核、预览无写入、异常排序及保存后状态核查失败。
- H5 与支付宝小程序构建通过，发布范围及规范副本检查通过。真实 uni-app 组件的桌面及 390px 窄屏预览、详情展开已核查；窄屏四个重量列实际坐标一致。正式 Chrome 登录后输入三瓶总重并仅点击预览，192 显示 09-23 出瓶及 09-22 回瓶依据，358/228 无预警，重量三列及详情展开正常；验收输入已清空。
- 受控证据位于隔离工作区 `outputs/trust-audit/2026-09-24-filling-warning/`，并在主工作区同路径保存：源单/流转只读快照、完整重放、线上验收、云端逐文件回读、网页哈希回读、版本清单及回退包。不进入 Git。
- 云函数回退包 `cloud-before.tgz` SHA-256：`73953d78dfc2b4610ad91890a7997c508d59c957d3c06635e94e2359a1dcaf6c`；网页回退包 `web-before.tgz` SHA-256：`6c136c157cfe77b7d82b91e611b0379da776f2ea4c09cb885596c4b3a14a648b`，旧网页版本 `1790221263381-e73255b8`。
- 回退仅恢复函数和网页包，再核对运行版本与资源；不回写历史业务数据。完整读取是有变化检测的实时读取，不是跨集合原子快照。

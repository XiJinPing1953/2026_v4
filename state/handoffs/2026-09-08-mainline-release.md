# 2026-09-08 主线发布基线与验收证据交接

## 目标与范围

本交接只收口发布完整性、可复现验证和生产/候选边界。没有部署、云端业务写入、客户数据修改、schema 变更或硬件操作；没有改动根 `STATE.md`、`AGENTS.md`、`docs/RULES.md` 或 `state/INDEX.md`。私有发布证据只读核对，未复制客户原始数据、凭据或日志进 Git。

## 生产与候选范围对照

| 对象 | 源码事实 | 部署与验收事实 |
|---|---|---|
| 旧共同基线 | `90336801cf67d7c31b9df7104de3c1545452285f` | 仅用于说明两条分支的共同祖先，不代表当前生产 |
| 当前已发布小范围源码 | `101068bb9580b6c0faa1b9b83886789d8b5c2de3`，从旧基线独立演进 | H5、`crm-customer-settlement`、`crm-haonuo-reconciliation` 及此前限定修正函数有发布/回读证据；两个客户修正、通用 `period_summary`、`opening_prepay`、页面和导出在各自限定范围已验收 |
| 系统可信度主线候选 | 本任务基线 `d9eb4fb9dc8edff82789a86a89631f4f86f70b20`，与生产提交的共同祖先仍是 `9033680` | 包含生产行为以及额外账务保护、灌装闭环、schema、页面和发布工具；全量候选未部署，不能把整仓状态写成生产状态 |

生产相对共同基线改动 27 个文件；候选中 26 个与生产提交逐字节相同。唯一有额外差异的是 `crm-customer-settlement/index.js`，因为候选还叠加全量可信度入口；生产语义是否保留以实际入口反例验证，不以文件相同推断。

## 生产证据复核

- 只读核对保存的发布证据：H5 `buildId=1788853708683-05ed825f`，源码提交 `101068b`，源码摘要 `9b8f9c7305ae482bd9e8c8273f61c5d8e4ad7c40827169cceb2d6584af0da5bb`，179 个产物文件摘要 `ea74e49966c86f325d1d0e9ab5ec5d2766415db47eccd62285ebdf7911ed8593`，目标为支付宝生产空间 `env-00jxuffegf2n`。
- 保存的两个云函数共 14 个文件哈希全部与 `101068b` 干净检出一致；记录明确数据库 schema 和硬件均未改变。云平台不暴露运行源码哈希，因此云函数对应关系由提交、上传文件哈希及真实接口行为共同支撑。
- 保存的线上版本回读与本地产物版本一致；发布后只读核查记录控制客户不变。生产页面实际下载的两种导出验证通过：期间汇总一致、4 个日期余额检查通过、24 张迁移销售均为已结清。这里仅记录证据结论，不纳入原始客户明细。
- 从全新临时克隆检出 `101068b`，执行交接记录的四个测试文件，19/19 通过且工作树保持干净。候选发布工具对该干净检出重新计算出的源码摘要与保存值完全相同。
- 历史生产提交本身没有后来加入的统一发布工具；源码摘要和保存产物可由当前候选中的 `releaseEvidence` 工具复验，但不能声称 `101068b` 单独包含完整的新发布工作流。

## 本次最小补齐

- `verifyTrust.cjs` 注册 `periodSummary`、多客户隔离、第二客户重建/期初预付款和迁移结清状态四组后续反例，防止测试文件存在但统一入口不执行。
- `createReleaseManifest.cjs` 生成清单前强制执行发布完整性检查；缺少云函数本地辅助文件、公共副本漂移、ACL 副本漂移或脏源码时不再产生可误用的正式候选清单。
- 公共 `financialRead` 的 `crm-haonuo-reconciliation` 部署副本加入统一副本清单。现有内容与公共源码一致；本次只补持续检查，没有改业务代码。
- 新增一个针对“已提交云函数缺辅助文件仍生成清单”的发布反例；没有扩展到账务、灌装、schema 或硬件测试范围。

## 可复现命令与结果

```bash
# 在 101068b 的干净检出中复现生产业务源码
node --test scripts/haonuoReconciliation.test.cjs scripts/periodSummary.test.cjs scripts/accountingReconciliationIntegration.test.cjs scripts/reconciledSaleStatus.test.cjs

# 在本次候选提交的干净检出中执行统一入口及两种产品检查
npm test
node scripts/checkReleaseIntegrity.cjs --product=web --require-clean
node scripts/checkReleaseIntegrity.cjs --product=cloud --require-clean

# 仅在确认下一批明确部署范围后生成候选清单；生成不等于部署或验收
node scripts/createReleaseManifest.cjs --product=cloud
```

修改前证据：旧统一入口 72/72 通过，但没有执行上述四组后续反例；单独补跑遗漏范围 18/18 通过。生产干净检出 19/19 通过。修改后的已提交候选在全新克隆中统一入口 91/91 通过，web/cloud 干净发布检查通过；云候选清单记录 8 个函数、13 个显式数据库文件，结算函数的 `periodSummary.js`、`receiptSource.js` 和公共读取副本均在清单内，状态仍明确为 `not_deployed` / `not_verified`。主任务合并后仍须复验。

## 下一批发布必要检查

1. 以合并后的已提交候选为发布源，确认生产 `101068b` 的结算、期初预付款、专用函数及前端行为没有在解决冲突时丢失；不能因 Git 历史不相交而整仓覆盖生产。
2. 先运行统一入口和 web/cloud `--require-clean` 检查，再审查生成清单中的目标环境、精确函数、辅助文件及显式 schema/index。`crm-customer-settlement` 发布包必须同时包含 `periodSummary.js`、`receiptSource.js` 和公共读取副本。
3. 专用修正批次已经完成，不得重跑。若下一批不需要重新上传专用修正函数，应在发布范围和回执中明确“保持现有运行版本”；若要上传，必须把函数加入明确范围并重新核对完整文件哈希。
4. H5 重新构建后记录提交、源码摘要、产物摘要和目标空间；上传后回读版本及所有静态文件，再登录验证受影响入口。云函数分别保存上传文件清单，并以认证后的业务反例验收；发布成功不能替代业务验收。
5. schema/index、PDA、Windows 网关和现场设备按各自产品单独发布与验收。不得把 H5 构建或本地模拟当作云事务、真机扫码称重、监管入队或设备联锁证据。

## 仍未验证

- 本任务未进行任何新部署、生产写入或全仓客户审计；系统可信度全量候选仍未上线。
- 四项长期改进指标均未完整采集；旧 38 张只保留为 2026-09-05 时点基线，两个客户已处理不等于全仓差异为零。
- 云平台不提供运行函数源码哈希；当前只能用上传文件证据和真实行为回读建立对应关系。
- 灌装真实云事务/调度、PDA 原生安装、Windows 网关及现场硬件仍需主任务按产品完成发布和验收。

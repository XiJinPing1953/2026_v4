{
  "bsonType": "object",
  "description": "Regulatory ingest idempotency keys",
  "additionalProperties": false,
  "required": [
    "station_id",
    "payload_type",
    "dedup_key",
    "status",
    "request_id",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "_id": { "bsonType": "string" },
    "station_id": { "bsonType": "string" },
    "payload_type": { "bsonType": "string", "enum": ["snapshot", "event"] },
    "dedup_key": { "bsonType": "string" },
    "status": { "bsonType": "string", "enum": ["processing", "accepted"] },
    "request_id": { "bsonType": "string" },
    "created_at": { "bsonType": "number" },
    "updated_at": { "bsonType": "number" }
  },
  "permission": {
    "read": false,
    "create": false,
    "update": false,
    "delete": false
  },
  "indexes": [
    { "key": { "dedup_key": 1 }, "name": "uniq_dedup_key", "unique": true },
    { "key": { "station_id": 1, "payload_type": 1, "created_at": -1 }, "name": "idx_station_payload_created" }
  ]
}
